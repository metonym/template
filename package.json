{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "bun run --filter server start",
    "dev": "bun --filter '*' dev",
    "build": "bun --filter '*' build",
    "typecheck": "bunx tsc",
    "lint": "biome lint .",
    "lint:fix": "biome check --write --unsafe ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.7",
    "lefthook": "^1.13.6",
    "typescript": "^5.9.3"
  }
}

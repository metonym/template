{
  "name": "esbuild-lib",
  "version": "0.0.1",
  "license": "MIT",
  "description": "<< >>",
  "author": "Eric Liu (https://github.com/metonym)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "dev": "esbuild src/index.ts --outfile=dist/index.js --watch",
    "prepack": "tsc && esbuild src/index.ts --outdir=dist --bundle --format=cjs"
  },
  "devDependencies": {
    "esbuild": "^0.11.0",
    "typescript": "^4.2.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/metonym/template"
  },
  "homepage": "https://github.com/metonym/template",
  "bugs": "https://github.com/metonym/template/issues",
  "keywords": [
    "esbuild"
  ],
  "files": [
    "dist"
  ]
}

{
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "start": "bun run --filter server start",
    "dev": "bun --filter '*' dev",
    "build": "bun --filter '*' build",
    "typecheck": "bunx tsc",
    "check": "bunx biome check --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "lefthook": "^1.12.3",
    "typescript": "^5.9.2"
  }
}
